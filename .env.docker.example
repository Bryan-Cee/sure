# Local Docker Development Environment Configuration
# Copy this file to .env and customize as needed

# Database Configuration
POSTGRES_USER=maybe_user
POSTGRES_PASSWORD=maybe_password
POSTGRES_DB=maybe_production

# App Security (generate a new one for production)
# Generate with: openssl rand -hex 64
SECRET_KEY_BASE=a7523c3d0ae56415046ad8abae168d71074a79534a7062258f8d1d51ac2f76d3c3bc86d86b6b0b307df30d9a6a90a2066a3fa9e67c5e6f374dbd7dd4e0778e13

# Ollama Configuration (Local LLM - Recommended)
# Make sure Ollama is running on your host machine: `ollama serve`
# Download a model first: `ollama pull llama3.2`
OLLAMA_BASE_URL=http://host.docker.internal:11434

# OpenAI Configuration (Optional - Costs Money!)
# Only needed if you want OpenAI as fallback or don't have Ollama
# OPENAI_ACCESS_TOKEN=your_openai_api_key_here

# Market Data (Optional - Uses API Credits)
# TWELVE_DATA_API_KEY=your_twelve_data_api_key

# Self-hosting flag (should stay true for local instances)
SELF_HOSTED=true
